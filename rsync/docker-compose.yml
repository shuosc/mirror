version: "2"
services:
   rsync:
     image: shuosc/rsync:latest
     ports:
       - "873:873"
     volumes:
       - /home/zhonger/ftp:/var/www/data
       - ./logs:/var/log/rsync
       - ./:/opt/rsync
     restart: always
     privileged: true